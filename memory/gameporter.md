---
name: gameporter
description: "GamePorter — yan's own native macOS Windows-game launcher (Whisky/CrossOver replacement) built on Apple GPTK D3DMetal; repo ~/GamePorter, installed /Applications/GamePorter.app"
metadata: 
  node_type: memory
  type: project
  originSessionId: 22b83917-f49a-48b7-afd3-2c6b4396baad
---

GamePorter (created 2026-07-06): personal-use native SwiftUI app replacing CrossOver/Whisky. Repo `~/GamePorter` → private GitHub `etedgy/GamePorter`, `./build.sh` → /Applications/GamePorter.app (ad-hoc signed).

- Runtime: `~/Library/Application Support/GamePorter/` — `Toolkit/` holds the Gcenx game-porting-toolkit 3.0-3 build (x86_64 Wine + Apple D3DMetal.framework, runs under Rosetta; wine at `Toolkit/Game Porting Toolkit.app/Contents/Resources/wine/bin/wine64`), `Bottles/<uuid>/` = Wine prefixes + `gameporter.json`, `Logs/`.
- App auto-downloads the toolkit from Gcenx GitHub releases if missing; SetupView also imports Apple's official GPTK dmg (redist/lib overlay) to upgrade D3DMetal — user has an Apple dev account.
- Features: bottles (win10 default), run exe (`wine64 start /unix`), .msi via msiexec, one-click Steam installer, program auto-discovery + pinning, toggles ESYNC / MTL_HUD_ENABLED / RetinaMode registry / ROSETTA_ADVERTISE_AVX, tools (winecfg/taskmgr/regedit/uninstaller), wineserver -k kill, uninstall (PR #1: parses system.reg Uninstall keys; QuietUninstallString → `msiexec /x{GUID} /qn` → cmd /c UninstallString → uninstaller --remove).
- Gotcha learned live: MSI UI dialogs (even /qb) fail to render under this Wine build and hang invisibly — always use /qn for msiexec; wine GUI processes inherit com.yan.gameporter bundle id.
- CrossOver-style resource handling (PR #2): WINE_LARGE_ADDRESS_AWARE=1 default env + RLIMIT_NOFILE raised to 65536 at app launch (children inherit; Finder apps default to 256). Installer-crash detection: scans wine log for `alloc_pages_vprot`/"Assertion failed" → surfaces plain-language message instead of silent quit.
- FitGirl-repack finding CORRECTED (2026-07-06): `alloc_pages_vprot` crash is a Wine 7.x bug FIXED in newer Wine — NOT a hard limit. CrossOver 26.1 uses Wine 11.0 and installs the repack fine (user confirmed). Verified: THPS setup.exe crashes on our GPTK Wine 7.7 but gets PAST the assertion on Gcenx wine-staging 11.10. Root cause was the stale engine, not memory env. User owns THPS 1+2 on Xbox (legit), couldn't get Mac version.
- Multi-engine + multi-renderer (PR #3): app now installs/holds multiple Wine engines side by side and picks engine+renderer PER BOTTLE. Engines under `Engines/<id>/` (+ legacy GPTK under `Toolkit/`). Catalog: Gcenx macOS_Wine_builds wine-staging-11.10 (newest, vanilla, DXMT/DXVK/WineD3D) + game-porting-toolkit 3.0-3 (GPTK, D3DMetal/DXVK/WineD3D). NOTE: Wine 11 loader binary is `wine` (not `wine64`); EngineManager.findLoader handles both. Renderers via WINEDLLOVERRIDES + RendererStager staging dlls into prefix (x86_64-windows→system32, i386-windows→syswow64): DXVK=Gcenx DXVK-macOS v1.10.3 (only d3d10core+d3d11, dxgi stays builtin), DXMT=3Shain/dxmt v0.80 (needs Wine 10.18+, winemetal.so via WINEDLLPATH to x86_64-unix dir, +MetalFX). Vanilla wine-staging does NOT bundle Apple D3DMetal (that's GPTK-only). Files: Models/Engine.swift, Managers/EngineManager.swift, Managers/RendererStager.swift, Views/EnginesView.swift; Bottle.engineID/renderer are optional (Codable-safe migration).
- Installer crashes on Wine 11 — 3 causes FIXED (PR #4): (a) game renderer (DXMT/DXVK) DLL overrides + winemetal forced onto 32-bit installers → `mmap() Cannot allocate memory` at high 0x7fff addrs → stack overflow; installers/winecfg/prefix-ops now run `plainGraphics=true` (no d3d overrides) via WineRunner.run(plainGraphics:). (b) wedged wineserver from a prior crash breaks all later runs with same mmap errors → runInstaller kills the bottle's wineserver first. (c) cross-version prefix (created on 7.7, switched to 11) crashes → setEngine runs `wineboot -u`. Verified: Notepad++ NSIS installer installs clean on Wine 11 plain-graphics (exit 0). Rule: NEVER apply game renderer overrides to installers/tools — only to actual game launches.
- REAL root cause of THPS repack crash (2026-07-06): setup.exe is 32-bit InnoSetup. Gcenx wine-staging 11.10 uses "new WoW64" → 32-bit RtlUnwindEx unwind loop → stack overflow (crashes at LOAD, 1 log line). GPTK 7.7 dies earlier on decompression (alloc_pages_vprot). CrossOver's patched wine handles 32-bit properly. My "Wine 11 fixes it" was WRONG — based only on the decompression stage, never a completed install.
- CrossOver engine (PR #5): EngineManager.detectCrossOver() auto-detects /Applications/CrossOver.app and exposes its `bin/wineloader` as a selectable engine (kind .crossover, D3DMetal/DXVK/WineD3D), driven against GamePorter's OWN prefixes (NOT CrossOver bottles) via Engine.extraEnv = WINELOADER/WINESERVER/WINEDLLPATH(lib/wine:lib64/wine)/DYLD_FALLBACK_LIBRARY_PATH(lib:lib64), applied in WineRunner.environment. The perl `bin/wine` wrapper needs a CX bottle ("bottle 'default' not found") — must use `bin/wineloader` binary directly. Verified: installs THPS repack, extracts C:\THPS\{Base,Engine,_Redist} 376MB+, no crash. Needs a valid CrossOver license (user's own install, personal use).
- "Access denied" install fix (PR #6): FitGirl/repack installers default the install dir to the drive with MOST FREE SPACE. Z: maps to macOS root / (huge free space) → they pick Z:\Games = / → Error 5 Access denied. Fix: mark Z: as network drive (`reg add HKLM\Software\Wine\Drives /v z: /t REG_SZ /d network /f`) → installers skip it, default to C:\Games (inside bottle, writable). Verified via InnoSetup /LOG. WineRunner.configureDrives() called in createPrefix/updatePrefix + before each installer (self-heals old bottles). SystemDrive is correctly C: — the issue is free-space heuristic, not {sd}.
- REPACK/SCENE INSTALL WORKFLOW (works, 2026-07-06): FitGirl FreeArc installer HANGS under ALL Wine-on-Mac tools (unarc.dll deadlocks under Rosetta — confirmed CrossOver-the-app hangs identically; 100% CPU, 0 bytes read). Solution = use a SCENE release with a normal installer instead: TENOKE THPS1+2 = 98-vol RAR (.rar/.r00..) → `brew install unar`; `unar -f first.rar` (point at .rar not .r00) → 23GB ISO → `hdiutil attach` → SETUP.exe (4MB) + SETUP.bin (24GB obfuscated blob, NOT extractable directly) + Crack/. SETUP.exe is a normal GUI installer (idle 0% CPU waiting for clicks, NOT the FreeArc 100% spin — check this to predict) → runs fine via CrossOver engine, installs 23GB to C:\Games. Then apply crack: copy Crack/* over install. Diagnostic: FitGirl setup has ISDone.dll/unarc.dll/FreeArc strings (bad); TENOKE SETUP.exe has none (good). FitGirl also defaults install to Z:\Games (free-space heuristic → macOS root → Access denied); mark Z: network drive to force C:.
- Game launchers (PR #7, CrossOver-style): discoverGames() parses Desktop/Start-Menu .lnk (Shell Link LocalBasePath at LinkInfo+16) → maps Win path to bottle → shows Play launcher in Games section. prettyName() cleans SCENE-CAPS. + Add Game… button (pin any exe). + reload() recovers bottles that lost gameporter.json (rebuilds metadata from the drive_c prefix — a bottle DID lose its json and vanished while the 23GB game sat safe on disk; game installs to C:\Games so also added that to discoverPrograms scan).
- Gotcha: quitting GamePorter via `osascript quit` may not fully relaunch (focuses old instance) — `killall -9 GamePorter` before `open -a` when testing new builds. Bottle folder name = UUID = Bottle.id.uuidString (uppercase).
- Engine guidance: Wine Staging 11.10 = most games + non-repack/Steam/GOG installers (DXMT/DXVK). CrossOver = 32-bit repack/InnoSetup installers CrossOver-only. GPTK = D3DMetal on old wine. Installed game is usually 64-bit → runs fine on Wine 11 after install.
- Component versions to bump over time: check Gcenx/macOS_Wine_builds (was 11.10, June 2026), 3Shain/dxmt (was v0.80), Gcenx/DXVK-macOS (v1.10.3, old but the macOS-compatible one). Kegworks renamed → Sikarugir (wrapper app, not a droppable engine).
- Verified end-to-end 2026-07-06: bottle created via UI, winecfg launched. Known quirk: first Create click in the NewBottleSheet sometimes needs a second click.
- CrossOver.app also installed on this Mac (separate, unrelated).
- Researched roadmap (2026-07-06, user approved direction, not yet built): bottle bootstrap (vc_redist silent + corefonts + CurrentBuild=19042 spoof), winetricks panel, DXVK-macOS v1.10.3-async toggle (Gcenx builtin tarball, matches our CX22 wine ABI; covers DX9/10 which otherwise hit slow WineD3D/OpenGL), per-game .app shortcuts, Fix-Steam button (steam.cfg BootStrapperInhibitAll + launch args), D3DM shader-cache clear (`$(getconf DARWIN_USER_CACHE_DIR)/d3dm`), bottle snapshots, env-var/launch-args editors. Our wine = esync only (msync tested, unsupported); DXMT needs newer wine ABI — experimental/later.
- This memory file lives in-repo at `memory/gameporter.md`, symlinked from `~/.claude/projects/-Users-yan/memory/gameporter.md`; commit+push after memory updates.

Related: [[aiscope]] [[local-coder-stack]]
- C&C3 Tiberium Wars install (2026-07-08, autonomous): FairLight .iso (flt-cnc3.iso) = 2007 RETAIL disc, InstallShield 12 + cnc3kane.msi + .cab game data + FairLight/RetailExe/1.0/cnc3game.dat crack (SafeDisc bypass). InstallShield silent install WORKS headless + no CD-key block: `wineloader "Z:\Volumes\CDROM\setup.exe" /s /v"/qn INSTALLDIR=\"C:\Games\CnC3\" REBOOT=ReallySuppress"` → 5.6GB English install, installs d3dx9 redist. Crack: replace <game>/RetailExe/1.0/cnc3game.dat with FairLight's. GOTCHA: CNC3.exe (launcher/Control Center) exits immediately w/o handing off; the REAL game binary is RetailExe/1.0/cnc3game.dat — copy to cnc3game.exe (wine start /unix won't exec a .dat) and pin THAT. Smoke test: cnc3game.exe stays alive no crash (full render unverified — screen was locked). Bottle: CrossOver engine + WineD3D renderer (DX9 game, D3DMetal is DX11/12 only) + winxp64 (2007 game). Bottle id 8BE94D87.
- Disc-image mounts show at their volume label (/Volumes/CDROM here) not a predictable name — grep `mount` output for the right one; `hdiutil detach` when done.
- C&C3 LAUNCH SOLVED (2026-07-08): SAGE-engine games (C&C3/RA3/C&C4) MUST launch via their real launcher CNC3.exe (a .NET Control Center that reads the .SkuDef and mounts the .big archives itself). Do NOT launch the raw game binary (RetailExe\1.0\cnc3game.dat) directly — the manual "-config <each config.txt> -add-search-path big:" bypass loads but stalls (30MB, doesn't fully mount .big). CrossOver's bottle HAS .NET Framework (v1.1-v4.0) so CNC3.exe runs fine. Pin CNC3.exe with workingDir=install root (C:\Games\CnC3). The .NET launcher was my blind spot — I wrongly thought "CNC3.exe exits immediately" early on (it actually runs). Crack = FairLight cnc3game.dat replacing RetailExe\1.0\cnc3game.dat (SafeDisc bypass). Renderer WineD3D (DX9 game).
- GamePorter PinnedProgram now has workingDir + arguments (WineRunner.launch direct-invokes exe with cwd when workingDir set, else start /unix). Useful general feature for games needing a specific CWD.
- C&C3 splash-hang / won't-launch FIX (2026-07-08, via CodeWeavers tip): (1) DLL override `dinput8=native,builtin` (`reg add HKCU\Software\Wine\DllOverrides /v dinput8 /d native,builtin`) — CrossOver's documented C&C3 "if it won't start" fix. (2) A crash corrupts the game config → reset AppData\Roaming\Command & Conquer 3 Tiberium Wars\Profiles\<user>\Options.ini (rename the dir, game regenerates defaults). After both, cnc3game loads properly (mem climbs 230→640MB = reaching menu; stuck-splash was flat 30MB). Source: codeweavers.com C&C3 tips.
- killAll HARDENED: `wineserver -k` leaves crashed/DETACHED zombies (they wedge next launch = stuck splash). WineRunner.killAll now also `ps -Aeww` → SIGKILL any proc whose env has WINEPREFIX==this bottle (scoped, won't touch other bottles). launch() now calls killAll before every game start (self-heal). User hit this: game crashed mid-mission → zombie CNC3.exe+cnc3game.dat held prefix → splash stuck.
- C&C3 GRAPHICS on Mac (2026-07-08): max/high SHADER detail CRASHES because WineD3D translates DX9 shaders via macOS's buggy OpenGL/GLSL (AppleGamingWiki: shaders disabled on Mac by default). Stable good-looking config = everything high (textures/models/water/VFX/terrain/animation/decal) EXCEPT Shader Detail LOW + Shadow Detail low + AntiAliasing off. Written to Profiles/<user>/Options.ini (StaticGameLOD=Custom, 1920x1080, UseShadowVolumes=no, ShadowDetail=1). Loads clean (mem 300→560MB). The TRUE max-shaders fix = DXVK d3d9 (DX9→Vulkan→Metal, bypasses OpenGL). CrossOver HAS a DXVK d3d9.dll (lib/dxvk/{i386,x86_64}-windows/d3d9.dll, confirmed dxvk::D3D9DeviceEx) + libMoltenVK. But dropping the dll + d3d9=native override ALONE didn't init (stuck 125MB, no Vulkan in log) — CrossOver's DXVK needs its full Vulkan/MoltenVK/winevulkan environment set up (a real project, not a dll swap). Parked. Source: applegamingwiki.com C&C3.
- DXVK-for-DX9-on-Apple-Silicon PROJECT (2026-07-08) — DEFINITIVE FINDING: DX9→DXVK→MoltenVK→Metal is the ONLY good-shader path (D3DMetal is DX11/12 only; WineD3D→OpenGL has broken shaders on Mac). Proven: DXVK d3d9 LOADS + MoltenVK 1.4.1 inits on the Gcenx Wine 11.10 engine (which BUNDLES libMoltenVK.dylib + winevulkan.so). BUT the wall: UPSTREAM DXVK (all versions incl v3.0.1) HARD-REQUIRES `geometryShader`, which Apple Metal fundamentally lacks → "Skipping: Device does not support required feature 'geometryShader' → No adapters found → terminate DxvkError". Only a PATCHED DXVK fork (geometryShader optional) works on Mac. Options tried: (a) Gcenx DXVK-macOS v1.10.3-20230507 non-repack HAS d3d9 but unpatched → VK_ERROR_FEATURE_NOT_PRESENT (5th flag=geometryShader, 39th=float64) device-fail. (b) Gcenx repack build IS patched but STRIPPED d3d9. (c) CrossOver's DXVK d3d9 IS patched (0 feature errors!) but proprietary + ABI-tied to CrossOver wine — doesn't load on Gcenx Wine 11 (no DXVK log, fell to builtin, 116MB stall). (d) upstream DXVK 3.0.1 → same geometryShader crash. CONCLUSION: no FREE patched d3d9-DXVK exists for Apple Silicon. To unlock max DX9 shaders: build DXVK-macOS from source with d3d9 enabled + geometryShader made optional (real project), OR use CrossOver-the-app directly. For now C&C3 stays on CrossOver-engine + WineD3D + shaders-low (loads to 520MB menu, good-looking otherwise). Sources: dxvk.org, MoltenVK#203, applegamingwiki CrossOver.
- DXVK-for-DX9 SOLVED — BUILT OUR OWN (2026-07-08, PR/commit 614d0fd): forked Gcenx/DXVK-macOS (~/DXVK-macOS), patched src/d3d9/d3d9_device.cpp GetDeviceFeatures: geometryShader/shaderClipDistance/shaderCullDistance = `supported.core.features.X` instead of `VK_TRUE` (Metal lacks geometryShader+cullDistance → stock DXVK "No adapters found"). Also removed redundant _D3DDEVINFO_RESOURCEMANAGER typedef in d3d9_include.h (current mingw defines it → build error). Toolchain: brew install meson ninja mingw-w64 glslang; build: `meson setup --cross-file build-win32.txt --buildtype release --strip build.w32 && ninja -C build.w32` (+ win64). Built dlls (d3d9/d3d10core/d3d11/dxgi x32+x64) bundled in GamePorter Resources/dxvk (build.sh copies to app), RendererStager.bundledDXVK stages them for DXVK renderer, override now d3d9,d3d10core,d3d11,dxgi=n. Patch in patches/dxvk-apple-silicon-d3d9.patch. VERIFIED: C&C3 UltraHigh shaders renders via DXVK->MoltenVK->Metal (D3D9DeviceEx swapchain, 0 feature errors, 750MB loaded) — the config that crashed WineD3D. Best on CrossOver engine (reliable .NET launcher for CNC3.exe) + DXVK renderer; Wine 11 engine's wine-mono launcher is flaky for C&C3.
- DXVK black-screen-with-cursor on Mac (fullscreen present fails on wine Mac driver): fix = Wine VIRTUAL DESKTOP (windowed) — reg HKCU\Software\Wine\Explorer Desktop=Default + Explorer\Desktops Default=1920x1080; also AntiAliasing off (MSAA breaks on MoltenVK). Candidate GamePorter feature: a "Windowed (virtual desktop)" per-bottle toggle. [SUPERSEDED — see next entry, virtual-desktop was NOT the fix.]
- DXVK black-screen ROOT CAUSE CORRECTED (2026-07-08) — the virtual-desktop theory above was WRONG. Real cause = MoltenVK FAILS to compile DXVK-d3d9 shaders that bind multiple sampler types (2D/3D/cube/shadow) to one texture register: `[mvk-error] use of undeclared identifier 's0_2d_shadowSmplr'/'s0_3dSmplr'/'s0_cubeSmplr' → Fragment shader could not be compiled into pipeline → DxvkGraphicsPipeline: Failed to compile pipeline`. Cursor drew (simple shaders/splash ok) but menu 3D shaders all failed = black; DXVK then spins 100% CPU retrying. FIX = DXVK option `d3d9.forceSamplerTypeSpecConstants = True` (resolves real sampler type via spec constant at draw time instead of emitting all variants MoltenVK can't declare). VERIFIED: pipeline errors many→0. BAKED INTO APP: RendererStager.writeDXVKConf() writes `<bottle>/dxvk.conf` on DXVK stage; WineRunner sets DXVK_CONFIG_FILE to it (works regardless of cwd). Every DXVK bottle now auto-gets it. This makes our DXVK genuinely compile DX9 shaders on Apple Silicon — the public Mac forks (Gcenx/Whisky) deleted d3d9.dll entirely so they can't. Diagnostic method that cracked it: read the DXVK log (`Logs/<bottle>-*.log` or launch via CLI with WINEDLLOVERRIDES=d3d9,d3d10core,d3d11,dxgi=n) + `sample <pid>` for hot stack + watch %CPU (99%=shader-compile spin, 0.1%=idle/blocked-wait, moderate=rendering).
- C&C3 on DXVK — SEPARATE game-specific wall (2026-07-08): with shaders now compiling (0 errors), C&C3 still won't render — it PARKS in a blocking wait BEFORE calling CreateDevice (DXVK log ends at adapter-caps enum + "Process set as DPI aware", no swapchain), identical in windowed(-win) AND fullscreen, 0.0% CPU flat, main thread idle in Wine msg loop (`__wine_main→mach_msg`), no audio/network/movie symbol in sample. A C&C3 startup hang inside the game's own init, NOT a DXVK/present/activation issue (fullscreen didn't help). Intro movies are in .big archives + play AFTER CreateDevice so not the cause. SAGE `-win` flag must reach cnc3game.dat directly (CNC3.exe .NET launcher SWALLOWS it — only forwards `-config ...SkuDef`); launching cnc3game.dat directly with `-config C:\Games\CnC3\CNC3_english_1.0.SkuDef -win` does pass it but still parks. PARKED per user 2026-07-08.
- C&C3 RESTORED to playable (2026-07-08, user chose this over more DXVK digging): CrossOver engine + WineD3D + CNC3.exe launcher (no args) + d3d9=builtin + Options.ini ShaderDetail=0 (shaders-low avoids WineD3D max-shader crash). Bottle 8BE94D87. To restore engine after DXVK testing: edit gameporter.json (engineID/renderer/pin), flip user.reg d3d9→builtin, delete native DXVK dlls from system32/syswow64, reconcile prefix by running CrossOver `bin/wineloader wineboot -u` with env WINELOADER/WINESERVER/WINEDLLPATH/DYLD_FALLBACK_LIBRARY_PATH set (wineboot regenerates the ~185KB builtin d3d9 stub), then restart GamePorter.
- GOTCHA (cost hours 2026-07-08): GamePorter loads each bottle's gameporter.json into memory at startup and launches from the in-memory pin — editing gameporter.json on disk while the app runs has NO effect until `killall GamePorter && open` it. Every "my fix did nothing" symptom traced to this.
- Our built DXVK reports `v1.10.3-20230507-async (macOS)` (base version string kept from Gcenx source). Loads + enumerates the M4 Max adapter (via our geometryShader patch) on BOTH wine-staging-11.10 (MoltenVK build 334) AND CrossOver (MoltenVK 290) engines. CrossOver's CLI `bin/wine` wrapper can't be driven standalone (needs a CX bottle → "bottle 'default' not found"); use its `bin/wineloader` binary + extraEnv, OR the wine-staging engine's normal `bin/wine` for CLI testing/profiling.
