# Private working memory — never publish (documents personal install workflows)
memory/
.build/
.DS_Store

# Build artifacts — distributed via GitHub Releases, not tracked
build/
Package.resolved.bak
